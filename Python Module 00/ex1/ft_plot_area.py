def ft_plot_area():
    """
    Asks the user for the length and width and prints the computed area
    """
    length = int(input('Enter length: '))
    width = int(input('Enter width: '))
    print(f"Plot area: {length * width}")
